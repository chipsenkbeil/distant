name: 'Tag latest'

on:
  push:
    branches:
      - master

jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run latest-tag
        uses: EndBug/latest-tag@latest
